@model Hotel_Sunrise.Models.Habitacion

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Habitacion</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="No_Habitacion" class="control-label"></label>
                <input asp-for="No_Habitacion" class="form-control" />
                <span asp-validation-for="No_Habitacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
