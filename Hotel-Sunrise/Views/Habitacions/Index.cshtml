@model IEnumerable<Hotel_Sunrise.Models.Habitacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.No_Habitacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.No_Habitacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HabitacionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HabitacionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HabitacionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
